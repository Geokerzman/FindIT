{% include 'inc/header.twig' %}
  <a href="{{ URLROOT }}/posts" class="btn btn-back"><i class="fa fa-chevron-left" style="color: aliceblue;"></i> Back</a>
  <div class="card card-body mt-5">
    <h2>Add Post</h2>
    <p>Create a post with this form</p>
    <form action="{{ URLROOT }}/posts/add" method="post" id="send-form">
      <div class="form-group">
        <label for="title">Title: <sup>*</sup></label>
        <div class="errorContainer"></div>
        <input type="text" name="title" class="form-control form-control-lg allInput {{ data.title_err ? 'is-invalid' : '' }}" value="{{ data.title }}">
        <span class="invalid-feedback">{{ data.title_err }}</span>
      </div>
      <div class="form-group">
        <label for="body">Body: <sup>*</sup></label>
        <textarea name="body" class="form-control form-control-lg {{ data.body_err ? 'is-invalid' : '' }}">{{ data.body }}</textarea>
        <span class="invalid-feedback">{{ data.body_err }}</span>
      </div>
      <input type="submit" class="btn btn-success" id="submit-btn" value="Submit">
    </form>
  </div>
{% include 'inc/footer.twig' %}
